# Optional. Copy to .env to override defaults.
# TARGET_ARCH: auto-detected from host (uname -m); set to override (arm64 or amd64).
# GH_OWNER, GH_TOKEN: required only for make push / make all.

# TARGET_ARCH=amd64
GH_OWNER=
GH_TOKEN=
